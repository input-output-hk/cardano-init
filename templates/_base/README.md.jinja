# {{ project_name }}

A Cardano protocol project generated by [cardano-init](https://github.com/aspect-build/cardano-init).

## Architecture

```
{{ project_name }}/
├── Justfile                    # Task orchestration
{%- if has_on_chain %}
├── {{ on_chain.dir }}/                  # {{ on_chain.tool_name }} ({{ on_chain.language }})
{%- endif %}
{%- if has_off_chain %}
├── {{ off_chain.dir }}/                 # {{ off_chain.tool_name }} ({{ off_chain.language }})
{%- endif %}
{%- if has_infra %}
├── infra/                      # Infrastructure services
{%- endif %}
{%- if has_testing %}
├── {{ testing.dir }}/                       # {{ testing.tool_name }} ({{ testing.language }})
{%- endif %}
{%- if has_formal_methods %}
├── {{ formal_methods.dir }}/          # {{ formal_methods.tool_name }} ({{ formal_methods.language }})
{%- endif %}
{%- if has_blueprint %}
├── blueprint/                  # CIP-57 Plutus blueprint (drop or build plutus.json here)
{%- endif %}
└── README.md
```

## Quick Start

```bash
just build    # Build all components (on-chain first, so the blueprint exists)
just test     # Build the blueprint, then run every component's tests in order
just clean    # Remove build artifacts
```
{%- if has_off_chain or has_testing or has_infra %}

## Development

`build`, `test`, and `clean` run from the top level. Some components also expose
a long-running `dev` — a watch mode, a local devnet, or a REPL. Not every
component has one (it's optional), so check with `just -f <dir>/Justfile --list`
and run it directly, e.g. `just -f <dir>/Justfile dev`.
{%- if has_off_chain %}

When a component provisions a local devnet, its `dev` writes the connection
details into `.env` and the off-chain component connects to it automatically;
with no local devnet it falls back to a public provider (Blockfrost). You never
wire this by hand — it follows the presence of `INDEXER_URL` in `.env`.
{%- endif %}
{%- endif %}
{%- if has_on_chain %}

## On-chain: {{ on_chain.tool_name }}

The `{{ on_chain.dir }}/` directory contains the smart contract logic written in {{ on_chain.language }}.

```bash
just build-on-chain
just test-on-chain
```
{%- endif %}
{%- if has_off_chain %}

## Off-chain: {{ off_chain.tool_name }}

The `{{ off_chain.dir }}/` directory contains the transaction building and submission logic written in {{ off_chain.language }}.

```bash
just build-off-chain
just test-off-chain
```
{%- endif %}
{%- if has_testing %}

## Testing: {{ testing.tool_name }}

The `{{ testing.dir }}/` directory contains integration tests using {{ testing.tool_name }}.

```bash
just test-integration
```
{%- endif %}
{%- if has_formal_methods %}

## Formal methods: {{ formal_methods.tool_name }}

The `{{ formal_methods.dir }}/` directory contains formal specifications verified by {{ formal_methods.tool_name }}.

```bash
just verify
```
{%- endif %}

## Network

This project targets the **{{ network }}** network.
