=== Justfile ===
# snap-project — Cardano protocol project
# Generated by cardano-init. Edit freely.
#
# The top level aggregates the tasks that terminate and compose: build, test,
# clean. Long-running / interactive tasks (watch modes, local devnets, REPLs)
# are per-component and optional — a component exposes `dev` only when it has
# such a mode. Run one directly, e.g. `just -f <dir>/Justfile dev`; list a
# component's targets with `just -f <dir>/Justfile --list`. See README.md.

default:
    @just --list

# --- Infrastructure (cardano-up) ---
# Provision the local stack (long-running): `just -f infra/Justfile dev`.
build-infra:
    @echo '{{BOLD + CYAN}}▶ infrastructure · building{{NORMAL}}'
    @just -f infra/Justfile build

test-infra:
    @echo '{{BOLD + CYAN}}▶ infrastructure · testing{{NORMAL}}'
    @just -f infra/Justfile test

# --- Aggregate tasks ---
build: build-infra
    @echo '{{BOLD + GREEN}}✓ Build complete{{NORMAL}}'

# Build the blueprint first (consumers read ../blueprint/plutus.json), then run
# each component's tests in role order. A fullstack `protocol` component is the
# blueprint producer, so it builds and tests first.
test: test-infra
    @echo '{{BOLD + BLACK + BG_GREEN}}  ✓  All tests passed  {{NORMAL}}'

clean:
    just -f infra/Justfile clean
    rm -f blueprint/plutus.json
    @echo '{{BOLD + GREEN}}✓ Clean complete{{NORMAL}}'

=== README.md ===
# snap-project

A Cardano protocol project generated by [cardano-init](https://github.com/aspect-build/cardano-init).

## Architecture

```
snap-project/
├── Justfile                    # Task orchestration
├── AGENTS.md                   # Guidance for AI coding agents (CLAUDE.md imports it)
├── infra/                      # Infrastructure services
└── README.md
```

## Quick Start

```bash
just build    # Build all components (on-chain first, so the blueprint exists)
just test     # Build the blueprint, then run every component's tests in order
just clean    # Remove build artifacts
```

## Development

`build`, `test`, and `clean` run from the top level. Some components also expose a long-running `dev` — a watch mode, a local devnet, or a REPL. Not every component has one (it's optional), so check with `just -f <dir>/Justfile --list` and run it directly, e.g. `just -f <dir>/Justfile dev`.

## Network

The active network is controlled by a single variable — `CARDANO_NETWORK` in [`.env`](.env) — which every component reads at runtime. This project is generated for **preprod**:

```bash
CARDANO_NETWORK=preprod
```
To target a different network, edit that one line to `preview`, `preprod`, or `mainnet` and re-run your component's `just` task. Public networks also require a provider API key (e.g. Blockfrost) — set it alongside `CARDANO_NETWORK` in `.env`.

The infrastructure stack provisions a **local** chain for the network named in `.env`. If you change `CARDANO_NETWORK` after the stack is already running, the existing `cardano-up` context keeps its original network — tear it down and bring it back up to re-provision:

```bash
just -f infra/Justfile clean
just -f infra/Justfile dev
```

=== AGENTS.md ===
# AGENTS.md — snap-project

This is a Cardano protocol project generated by [`cardano-init`](https://github.com/input-output-hk/cardano-init). The components below were wired together through a fixed **interface contract** (see *Golden rules*) — they compose through that contract, not through direct imports across directories.

## Stack

| Role | Tool | Language | Directory |
|------|------|----------|-----------|
| Infrastructure | Kupo, Ogmios | — | `infra/` |

## Golden rules (do not break these)

These invariants are what let the components compose. Preserve them in any change:

1. **Blueprint is the on-chain → off-chain contract.** The on-chain component's `build` writes the CIP-57 Plutus blueprint to **`blueprint/plutus.json`**. Every other component reads that file — never reach into the on-chain source to get script bytes or parameter shapes.
2. **`plutus.json` may be absent.** It only exists after an on-chain `build`. Consumers must **degrade gracefully** when it is missing, not crash.
3. **`.env` is the connection seam — never hand-wire it.** Whichever component provisions a local endpoint writes the standard vars (`INDEXER_URL`, `OGMIOS_URL`, `NODE_SOCKET_PATH`, …) into the root `.env` during its `dev`. Consumers read those keys and **fall back gracefully when blank** (e.g. to a public provider). Do not add bespoke cross-component config; react to the presence of a key, never to which tool set it.
4. **Every component is standalone and composable.** Each component `Justfile` exposes `build`, `test`, `clean` and must succeed on its own. `dev` is **optional and per-component** (only where a real watch/daemon/devnet mode exists) and is **never** aggregated at the top level.
5. **Components talk to the contract, not to each other.** There is no direct dependency between, say, `off-chain/` and `on-chain/` — only through `blueprint/` and `.env`.

## Layout

```
snap-project/
├── Justfile                    # Top-level task orchestration (build/test/clean)
├── AGENTS.md / CLAUDE.md       # This guidance
├── .env                        # Shared connection vars (the .env seam)
├── infra/                      # Infrastructure services (via cardano-up)
└── README.md
```

## Commands

Everything is driven by [`just`](https://just.systems). Run these from the root:

```bash
just build    # Build all components (on-chain / protocol first, so the blueprint exists)
just test     # Build the blueprint, then run each component's tests in role order
just clean    # Remove build artifacts (and blueprint/plutus.json)
```
`build`, `test`, and `clean` are the only **aggregated** targets — they terminate and compose. Long-running or interactive modes live per component and are **not** aggregated. Discover and run them directly:

```bash
just -f <dir>/Justfile --list     # see a component's targets
just -f <dir>/Justfile dev        # run its dev/watch/devnet mode (only where it exists)
```

## Components

### Infrastructure — `infra/` (via cardano-up)

Provisions the selected services and publishes their connection details to `.env`:

- **Kupo** — https://github.com/CardanoSolutions/kupo
- **Ogmios** — https://ogmios.dev

Requires Docker. See `infra/README.md` for the `just` targets.

## The worked example

Every generated project ships the **same** worked example — a **gift card**: a one-shot minting policy that mints a unique token gated by a specific UTxO, plus a `redeem` validator that releases a locked gift when the token is burned. On- and off-chain sides share the same parameter ABI through the blueprint, which is why the project builds and its tests pass out of the box. Use it as the template for real logic; replace it, don't accrete around it.

## Cardano knowledge: use cardano-dev-skills

For Cardano-specific work (writing validators, building transactions, debugging on-chain failures, tokens, governance), consult the [**cardano-dev-skills**](https://github.com/cardano-foundation/cardano-dev-skills) skill set rather than relying on model training data — Cardano tooling drifts fast. In Claude Code, install once:

```
/plugin marketplace add cardano-foundation/cardano-dev-skills
/plugin install cardano-dev-skills@cardano-dev-skills
```

Then run `/cardano-context` to persist Cardano directives into this project. (Other agents: read the `skills/*/SKILL.md` files directly, or symlink `skills/` into `.agents/skills`.)

**Skills most relevant to this stack:**

- `suggest-tooling`, `explain-eutxo`, `explain-cip` — orientation and the eUTxO model
- `setup-devnet` — local devnet / testnet setup and connection
- `query-chain` — choosing how to read chain data (Blockfrost / Ogmios / indexers)

## Gotchas

- **Don't hand-edit connection URLs in `.env`.** They're populated at runtime by whichever component provides a local endpoint (see Golden rule 3). Blank is a valid state — it means "use the public fallback."
- **`blueprint/plutus.json` is a build artifact**, not source. It won't exist until an on-chain build has run; regenerate it with `just build` rather than editing it by hand.
- **Keep the interface contract intact.** If you add or swap a component, make it read/write the same `blueprint/` and `.env` keys — that's what keeps the stack composable without per-pair glue.
- **Don't add a top-level `dev` target** or make one component import another's source. Cross-component coupling belongs in the contract, not the code.

=== CLAUDE.md ===
# CLAUDE.md

@AGENTS.md


=== .gitignore ===
# Build artifacts
/target/
build/
dist/
# Generated code (e.g. blueprint bundled into the off-chain library)
*.generated.ts

# Dependencies
node_modules/

# Environment
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

=== .env ===
# snap-project — environment configuration
# Generated by cardano-init. Edit freely.

CARDANO_NETWORK=preprod

# Chain connection details.
# Populated by whichever component provisions a local endpoint during its
# `just dev` — an infrastructure service, or a local devnet such as Yaci DevKit
# in the devnet role. Left empty otherwise, so consumers always find the keys
# at a stable, predictable location and degrade gracefully when they're blank.
INDEXER_URL=
INDEXER_PORT=
NODE_SOCKET_PATH=
# Endpoints populated when the matching infrastructure provider is provisioned.
OGMIOS_URL=
TX_SUBMIT_URL=
DOLOS_GRPC_URL=
CARDANO_NODE_API_URL=

=== infra/Justfile ===
# Infrastructure component (cardano-up) — provisions a local Cardano stack.
# Part of snap-project.
#
# This component lives at infra/, so the project root is one level up (../).
#
# The providers you selected with --infra are brought up together as a single
# cardano-up context named "snap-project":
#   - Kupo (kupo)
#   - Ogmios (ogmios)
#
# Requires Docker running and `cardano-up` on your PATH (see `cardano-init
# doctor`). The `-c/--context` flag targets this project's stack without
# touching your global active cardano-up context, so multiple projects coexist.

# Infrastructure is a runtime stack — there is nothing to build.
build:
    @echo "Nothing to build — infrastructure is a runtime stack. Run 'just dev' to provision it."

# Nothing to test without a running stack; kept as a no-op so the top-level
# `just test` stays green in CI (which has no Docker / running services).
test:
    @echo "Nothing to test — run 'just dev' to provision the stack."

# Provision the stack and publish its connection details to ../.env.
# Long-running: cardano-up brings up the Docker services for the context.
#
# Network is read from ../.env (CARDANO_NETWORK), defaulting to preview. Note:
# `context create` is a no-op if the context exists — `just clean` first to
# re-provision on a different network.
dev:
    net="$(grep -E '^CARDANO_NETWORK=' ../.env 2>/dev/null | head -n1 | cut -d= -f2- | tr -d '"')"; cardano-up context create snap-project -n "${net:-preview}" 2>/dev/null || true
    cardano-up install kupo --context snap-project
    cardano-up install ogmios --context snap-project
    cardano-up up --context snap-project
    sh scripts/write-env.sh
    @echo "Stack 'snap-project' is up. Connection details written to ../.env."

# Stop the stack's containers. Chain data is preserved (no re-sync next time);
# for a full teardown run `cardano-up uninstall <pkg> --context snap-project`.
clean:
    @cardano-up down --context snap-project
    @echo "Stopped the 'snap-project' stack."

=== infra/README.md ===
# Infrastructure (snap-project)

This component provisions a local Cardano infrastructure stack using
[`cardano-up`](https://github.com/blinklabs-io/cardano-up). The providers you
selected are managed together as a single cardano-up **context** named
`snap-project` (network: `preprod`):

- **Kupo** — cardano-up package `kupo`
- **Ogmios** — cardano-up package `ogmios`

## Prerequisites

- **Docker** running.
- **`cardano-up`** on your `PATH` — run `cardano-init doctor` for install help.

## Usage

```sh
just -f infra/Justfile dev      # provision the stack and write ../.env (long-running)
just -f infra/Justfile clean    # stop the stack (chain data is preserved)
```

`dev` creates/uses the `snap-project` context, installs the
providers (cardano-up resolves dependencies such as `cardano-node`
automatically), brings the Docker services up, and writes the connection
details into the project's `../.env`. Off-chain components read those values
(`INDEXER_URL`, `OGMIOS_URL`, `NODE_SOCKET_PATH`, …) and connect automatically.

The `-c/--context` flag scopes every command to this project's stack, so it
never disturbs your global active cardano-up context or other projects.

## Notes

- This component aggregates **all** `--infra` providers into one stack; there is
  no per-provider subdirectory.
- `cardano-up`'s state is machine-global (under `~/.config/cardano-up` and
  `~/.local/share/cardano-up`), not stored in this repo. The Justfile here is
  just a thin, project-scoped driver over it.

=== infra/scripts/write-env.sh ===
#!/usr/bin/env sh
# write-env.sh — translate cardano-up's context outputs into the project .env.
# Generated by cardano-init for snap-project.
#
# Run from infra/ (the dev recipe does this). It loads the cardano-up outputs
# for this project's context, then UPDATES the connection keys this stack owns
# in ../.env while preserving every other line — so keys seeded empty (e.g. an
# OGMIOS_URL on a kupo-only stack) stay present rather than being dropped.
set -e

CONTEXT="snap-project"
ENV_FILE="../.env"

# Load cardano-up's outputs (e.g. KUPO_URL, OGMIOS_URL, CARDANO_NODE_SOCKET_PATH)
# as shell variables. `context env` emits `export KEY=VALUE` lines.
eval "$(cardano-up context env --context "$CONTEXT")"

# Replace NAME=… in $ENV_FILE (append if absent); leaves all other lines intact.
set_key() {
    name="$1"
    value="$2"
    tmp="$(mktemp)"
    grep -v "^${name}=" "$ENV_FILE" > "$tmp" 2>/dev/null || true
    printf '%s=%s\n' "$name" "$value" >> "$tmp"
    mv "$tmp" "$ENV_FILE"
}

# CARDANO_NETWORK is left alone (user config in ../.env) — this script only owns
# the connection keys below, so a hand-edited network survives `just dev`.
set_key NODE_SOCKET_PATH "$CARDANO_NODE_SOCKET_PATH"
set_key INDEXER_URL "$KUPO_URL"
set_key OGMIOS_URL "$OGMIOS_URL"

